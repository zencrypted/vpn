{erl_opts, [debug_info, warn_unused_vars, warn_shadow_vars, warn_unused_import]}.

{deps, [
    tunctl
]}.

{relx, [
    {release, {vpn, "0.1.0"}, [vpn]},
    {dev_mode, true},
    {include_erts, false}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]}
    ]}
]}.
