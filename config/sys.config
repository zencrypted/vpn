[
  {kernel, [
    {logger_level, info}
  ]},

  {vpn, [
    {peers, [
      #{
        id => peer_a,
        name => <<"Peer A">>,
        remote_peer_id => peer_b,
        mode => tun,
        ifname => <<"tun0">>,
        ip => "10.20.20.1",
        local_udp_port => 5555,
        remote_ip => {127,0,0,1},
        remote_udp_port => 5556
      },

      #{
        id => peer_b,
        remote_peer_id => peer_a,
        mode => tun,
        ifname => <<"tun1">>,
        ip => "10.20.20.2",
        local_udp_port => 5556,
        remote_ip => {127,0,0,1},
        remote_udp_port => 5555
      }
    ]}
  ]}
].
